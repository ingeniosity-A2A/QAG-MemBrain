// Placeholder — EPOCH pillar (ArrowJS + GSAP animated UI layer)
import React from 'react';

export function AnimatedUI() {
  return <div className="animated-ui">[EPOCH Animated UI placeholder]</div>;
}
