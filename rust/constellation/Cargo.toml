[package]
name = "constellation"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Constellation — dynamic model routing layer for AMOS v2.1"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
# TODO: serde = { version = "1", features = ["derive"] }
# TODO: serde_json = "1"
# TODO: qnn-bridge = { path = "../qnn-bridge" }
