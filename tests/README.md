# Tests and Benchmarks

Validation for deterministic replay, Tashi consensus, and API contracts.

## Subdirectories
- /unit: JSONL, tween atoms, DAG validation
- /integration: JSONL -> Tashi -> temporal recall flows
- /benchmark: compression ratio, sync latency, replay speed

## Key Suites
- deterministic replay validation
- offline queue flush validation
- API contract compatibility
