[package]
name = "fapo"
version = "0.1.0"
edition = "2021"
description = "FAPO Selection — Fractional Pareto-Optimality for capability Arena selection"
license = "MIT OR Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tracing = "0.1"

[dev-dependencies]
tokio-test = "0.4"
